# entrepot

Le projet Spring Boot se trouve dans [demo](demo).

Pour le lancer depuis la racine du dépôt:

```powershell
mvn -f demo/pom.xml spring-boot:run
```

Ou bien:

```powershell
cd demo
mvn spring-boot:run
```